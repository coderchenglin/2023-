#include<stdio.h>

//写一个指向函数指针数组的指针

int main()
{
    int (*p[5])(int, int);  //函数指针数组

    int (*(*pp)[5])(int, int) = &p;  //指向函数指针数组的指针



    
    //p1:函数指针   p2:函数指针数组    p3:指向[函数指针数组]的指针
    int (*p1)(int,int);
    int (*p2[5])(int,int);
    int (*(*p3)[5])(int, int);

    return 0;
}

//总结:判断一个变量的类型,首先看他跟谁结合,判断他最终是一个什么
//如:没有(*p),先跟[]结合,他最终是一个数组,有(*p),首先他是一个指针
//接着把函数名以及他结合的东西,去掉,剩下的就是这个数组(或指针)的类型


